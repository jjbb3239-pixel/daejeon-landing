/**
 * 켜고 끄는 기능 스위치.
 *
 * 되돌리고 싶으면 여기 값만 false 로 바꾸면 된다.
 * 컴포넌트나 CSS 를 지울 필요 없다.
 */

/**
 * 모바일에서 기분 섹션 4개(#photo #food #cafe #course)를 접어둔다.
 *
 * 켜는 이유 : 방문자는 넷 중 하나만 필요한데 전부 쌓여 있어서
 *            모바일 페이지가 30화면(24,490px)까지 늘어났다.
 *            기분 섹션 4개만 16.3화면으로 전체의 54% 를 차지한다.
 *
 * 끄면 : 예전처럼 네 섹션이 전부 펼쳐진 상태로 이어진다.
 *
 * PC(760px 초과)는 이 값과 상관없이 항상 펼쳐진다. CSS 가 처리한다.
 */
export const COLLAPSE_MOOD_SECTIONS_ON_MOBILE = true;


/**
 * 리뷰 8장을 한 줄로 흘려보낸다 (오른쪽 -> 왼쪽).
 *
 * 켜는 이유 : 4묶음 8장을 쌓아두니 PC 1,269px(1.4화면),
 *            모바일 2,682px(3.3화면)을 먹는다. 한 줄이면 훨씬 짧다.
 *
 * 끄면 : 예전처럼 「01 PHOTO」같은 그룹 라벨과 함께 4줄로 쌓인다.
 *
 * 글이 있는 카드라 로고 배너보다 느리게 돌린다(40px/초).
 * 마우스를 올리거나 손가락으로 누르면 멈춘다.
 * prefers-reduced-motion 이 켜져 있으면 아예 안 움직이고 손으로 미는 줄이 된다.
 */
export const PROOF_MARQUEE = true;
